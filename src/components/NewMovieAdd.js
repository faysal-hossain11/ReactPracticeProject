import React from 'react';
import AddMovies from './AddMovies';

const NewMovieAdd = (props) => {
    return (
        <div>
            <h2>this is movies no:{props.movies}</h2>
        </div>
    );
};

export default NewMovieAdd;